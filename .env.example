# Port to expose on the host
PORT=8080

# Hugging Face Model ID (onnx-community/gte-multilingual-base is optimized for CPU inference)
MODEL_ID=onnx-community/gte-multilingual-base

# Data type precision (e.g. float32 for ONNX on CPU, float16 for CUDA/GPU)
DTYPE=float32

# Pooling strategy for embeddings (cls, mean)
POOLING=cls

# Hugging Face Text Embeddings Inference Docker Image
TEI_IMAGE=ghcr.io/huggingface/text-embeddings-inference:cpu-1.7

# TEI Performance & Memory Tuning (2048 prevents OOM on CPU/Docker Desktop)
MAX_BATCH_TOKENS=2048
MAX_CLIENT_BATCH_SIZE=8

# Optional Hugging Face Hub token (required for gated or private models)
# HF_TOKEN=hf_xxx
